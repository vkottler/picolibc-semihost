---
includes:
  - ../config/python/yambs/cflag_groups.yaml

default_target: picolibc-semihost

ldflag_groups:
  picolibc-semihost: [-lsemihost]

variants:
  picolibc-semihost:
    prefix: "arm-picolibc-eabi-"
    cflag_groups: [picolib]

dependencies:
  - github: {repo: coral, owner: &self vkottler}
    as_source: true
