# =====================================
# generator=datazen
# version=3.1.4
# hash=401131326017c6c7a73a4aeb770c9b19
# =====================================
---
project:
  name: picolibc-semihost

  github: {owner: &self vkottler}
  version: {major: 0, minor: 1, patch: 1}
variants:
  clang:
    suffix: &clang_version "-15"
    enabled: false

  # Disable regular targets.
  debug:
    enabled: false
  opt:
    enabled: false
  clang-opt:
    enabled: false

includes:
  - local/yambs.yaml
