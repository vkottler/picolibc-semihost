# =====================================
# generator=datazen
# version=3.1.4
# hash=d28d31404fc62d0ebce8ea1b4520c28f
# =====================================
---
project:
  name: picolibc-semihost

  github: {owner: &self vkottler}
  version: {major: 0, minor: 2, patch: 0}
variants:
  clang:
    suffix: &clang_version "-15"
    enabled: false

  # Disable regular targets.
  debug:
    enabled: false
  opt:
    enabled: false
  clang-opt:
    enabled: false

includes:
  - local/yambs.yaml
